using UnityEngine;

public class CarraraMusic : MonoBehaviour
{
    // Start is called once before the first execution of Update after the MonoBehaviour is created
    void Start()
    {
        MusicManager.Instance.PlayMusic("Carrara_Gameplay");
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
